A:折る。a*bの紙をb*(a-b)にできる。全てを出来る回数を答える。愚直にやるとTLEより/や%を使う。

B:一回のswapでハミング距離をminimamにする。diffに列挙し、diff-2 or diff-1 or diffになる。a-d -> d->aのようなペアがあるか、a-d->b-aのようなペアがあるかを調べる。答えがハミング距離だけでなく、indexを答えなければならないので最初にindexを保持しておく。
